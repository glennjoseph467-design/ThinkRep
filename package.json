{
  "name": "thinkrep",
  "version": "1.0.0",
  "description": "ThinkRep - Quiz-based cognitive training app",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "setup": "npm install && cd client && npm install",
    "deploy": "cd client && npx vite build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
